# COVERAGE.md — Route / module → feature SPEC audit map

The audit record that every route, screen, and backend module of the legacy app maps to a feature SPEC in
`specs/features/`. Built up as the `question-asker` loop documents each surface. The goal: when this is fully
ticked, we know the rebuild covers the legacy app 1:1 with nothing missed.

Legend: `[ ]` not yet documented · `[~]` SPEC in progress · `[x]` covered by a SPEC.

> Fresh scaffold — nothing documented yet. The checklists below are the surface inventory (from the
> research pass) to be filled in feature-by-feature.

## backend (Express API) — `backend`
- [x] auth (login/app/global, refresh, logout, register, change-password, delete-account, **forgot-password**, **reset-password**) → [auth SPEC](specs/features/auth/SPEC.md) (v0.4.0; incl. `middleware/auth.js` authz gates per D-C1; **NET-NEW `POST /forgot-password` per D-C4** + **`POST /reset-password` per D-C5** — privacy-safe recovery, request + reset steps, each a MINOR bump; the recovery path is now end-to-end)
- [x] members (CRUD, list) → [members SPEC](specs/features/members/SPEC.md) (v0.2.0; `DELETE /:id` cascade **wired** via shared `cascadeMemberDeletion` + Supabase auth-user delete per D-C1; `createMember` wired to Supabase `createUser` per D-C2)
- [x] programs (CRUD, archive, admin_only_data_entry) → [programs SPEC](specs/features/programs/SPEC.md) (v0.1.0; ported; `description` dropped per D-C2; notification emit deferred → `notifications` per D-C1)
- [x] program-memberships (members, details, role/status, remove, leave + `handleMemberExit`) → [program-memberships SPEC](specs/features/program-memberships/SPEC.md) (v0.1.0; ported; `createMemberAndEnroll` fixed→loginable per D-C2; `available`+`enroll` dropped as dead routes per D-C3; notification emits deferred per D-C4)
- [x] invites (invite, my-invites, all-invites, response, blocks) → [invites SPEC](specs/features/invites/SPEC.md) (v0.1.0; ported; co-mounted at `/api/program-memberships`; emits wired **live** per D-C2; `target_member_id` dropped per D-C3a; `getAllInvites` N+1 batched per D-C3b; accept-path membership write inline per D-C1)
- [x] workouts library (CRUD) → [workouts SPEC](specs/features/workouts/SPEC.md) (v0.1.0; 🏗️ ported — global library `/api/workouts` mounted; `POST /mobile` dropped as a byte-dup per D-C2; `consumed_by = [ios]`, GET-only live, admin CRUD called by neither client per D-REF; bare delete kept per D-S1/F2; program-scoped half → program-workouts)
- [x] program-workouts (custom CRUD, global+custom visibility toggles, merged list) → [program-workouts SPEC](specs/features/program-workouts/SPEC.md) (v0.1.0; 🏗️ ported — 6 routes at `/api/program-workouts`, the program-scoped half of the shared `workoutService.js`; `consumed_by = [web, ios]` all 6 routes 1:1; per-action admin authz **hoisted** to a `requireProgramAdmin` resolve-or-pass-through middleware per D-C2; faithful otherwise — merge/dual-id/lazy-materialization/dedup/in-use-guard kept per D-S1)
- [x] workout-logs (single, batch, member) → [workout-logs SPEC](specs/features/workout-logs/SPEC.md) (v0.1.0; 🏗️ ported — the `workoutLogRouter` half of the shared `routes/logs.js`/`services/logService.js`; `daily-health-logs` = the other half, appended to the same file pair later per D-C1; **2 dead GET routes dropped** — `GET /` + `GET /member/:memberName` called by neither client; 4 live routes ported. `consumed_by = [web, ios]` — trio 1:1, `POST /batch` web-only. 4 user-chosen cleanups: D-C2 positive-int duration, D-C3 collapse member-auth double-check, D-C4 de-dup membership lookups, D-C5 hoist the `admin_only_data_entry` lock to a `requireDataEntryAllowed` middleware. Faithful otherwise; F1–F9)
- [x] daily-health-logs (CRUD) → [daily-health-logs SPEC](specs/features/daily-health-logs/SPEC.md) (v0.1.0; 🏗️ ported — the `dailyHealthLogRouter` half of the shared `routes/logs.js`/`services/logService.js`, appended to the file pair `workout-logs` created. 4 routes (POST/GET/PUT/DELETE) at `/api/daily-health-logs`; `consumed_by = [web, ios]` all 4 routes 1:1, **no divergence**, no batch route. Faithful except 2 changes: D-C2 (reuse `workout-logs`' `requireDataEntryAllowed` middleware for the `admin_only_data_entry` lock — both halves enforce it identically), D-C3 (tidy `updateDailyHealthLog` to a single `body` param). One-row-per-day PK / 409-on-dup / sleep+diet validation / partial-update absent-vs-null / synthetic GET id kept; F1–F6)
- [x] notifications (unacknowledged, device register/unregister, broadcast, acknowledge, SSE stream) → [notifications SPEC](specs/features/notifications/SPEC.md) (v0.1.0 backend; ported; **replaced the deferred `utils/notifications.js` stub** so programs/memberships emits now fire; SSE stream auth migrated symmetric→Supabase JWKS per D-C2; APNs creds deferred per D-C4; `POST /broadcast` kept vestigial F1) — **WEB CLIENT now ported (run 48)**: replaced the `apps/web` `NotificationsGate` deferred stub with the faithful port (D-C5) — `EventSource` SSE client w/ `?token=`, `unacknowledged` backfill, single-notification modal queue (`NotificationModal`), `acknowledge`-on-confirm, per-notification React Query invalidation landing on the rebuilt keys; D-C6 `isAuthRoute` reconciled to include the net-new `/forgot-password`+`/reset-password`; D-C7 dropped the stray no-op `["program","roles",…]` key; **no new deps** (the 3 ported files only); a MINOR bump on the feature
- [x] analytics v1 (summary, participation, workouts/duration, timeline, distribution, types) → [analytics SPEC](specs/features/analytics/SPEC.md) (v0.1.0; 🏗️ ported — the `v1Router` half of the shared `routes/analytics.js`/`analyticsService.js` + the 2 analytics-only utils `dateRange.js`/`queryHelpers.js`; `analytics-v2` = the other half, appended later. **`participation/mtd` v1 dropped** (D-C2 — both clients use the v2 variant); 8 live routes at `/api/analytics`; `consumed_by = [web, ios]` all 1:1, no divergence. Read-only verbatim aggregation except 2 UTC cleanups: D-C3 (distribution weekday bucketing) + D-C4 (timeline labels). F1–F7)
- [x] analytics v2 (participation, workout-type aggregates) → [analytics-v2 SPEC](specs/features/analytics-v2/SPEC.md) (v0.1.0; 🏗️ ported — the `v2Router` half of the shared `routes/analytics.js`/`analyticsService.js`, appended to the v1 files; reuses the date/bucket helpers + the 2 utils landed with v1. **`GET /summary` (v2) dropped** (D-C2 — the mirror of v1's D-C2: both clients use the v1 summary; `getSummaryV2` dead); 5 live routes at `/api/analytics-v2` (participation/mtd + workouts/types/{total,most-popular,longest-duration,highest-participation}); `consumed_by = [web, ios]` all 1:1, no divergence. Faithful verbatim aggregation (D-S1); `getParticipationMTDV2` ≡ the v1 fn v1 dropped (now live), `getHighestParticipationWorkoutType`'s member-scoped branch dead. F1–F6)
- [x] member-analytics (metrics, history, streaks, recent) → [member-analytics SPEC](specs/features/member-analytics/SPEC.md) (v0.1.0; 🏗️ ported — **its own file pair** `routes/memberAnalytics.js` (4 routers) + `services/memberAnalyticsService.js` (4 fns + helpers), distinct from the analytics/analytics-v2 pair. 4 routes at `/api/member-{metrics,history,streaks,recent}`; `consumed_by = [web, ios]` all 4 routes 1:1, **no divergence**; `member-recent` is the shared workout-history read (why workout-logs dropped its 2 GETs). **Enforces per-program read authz** via `ensureProgramAccess` (F1 — unlike v1/v2). Faithful verbatim except D-C2 (re-export the 3 timeline helpers `resolveTimelineWindow`/`buildBuckets`/`bucketKey` from `analyticsService.js` — restores the legacy export surface; `depends_on: analytics`) + 2 cleanups: D-C3 (extract the shared requester-access prelude for history/streaks/recent, statuses 1:1) + D-C4 (guard null `program.start_date` in streaks). No UTC cleanup — dates already UTC-correct; F1–F7)
- [x] app-config (min iOS version) + push (APNs) → [app-config SPEC](specs/features/app-config/SPEC.md) (v0.1.0; 🏗️ ported — **owns app-config**: the inline `GET /api/app-config` returning `{ min_ios_version }` from the `MIN_IOS_VERSION` env, the iOS version gate; `consumed_by = [ios]` (web ignores it). Kept inline in `server.js` per D-C1; two pinned cleanups vs legacy — D-C2 `Cache-Control: public, max-age=300` + D-C3 trim/semver-validate `MIN_IOS_VERSION` (malformed → `null`). **Push (APNs) is referenced, not re-documented** — already owned by [notifications SPEC](specs/features/notifications/SPEC.md) (device routes `PUT`/`DELETE /api/notifications/device`, APNs dispatch, `member_push_tokens`, `APNS_*`) + [auth SPEC](specs/features/auth/SPEC.md) (login capture + `upsert/removePushToken`); §6 is the cross-reference index, `consumed_by = [ios]`. F1–F5. **Backend coverage now complete.**)

## web (Next.js) — `rasifiters-webapp`
- [x] public: **splash ✓** ([splash SPEC](specs/pages/web/splash/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[web]`, faithful D-S1), **login ✓** ([login SPEC](specs/pages/web/login/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[web]`, faithful D-S1 + ONE addition D-C1: "Forgot password?" link → `/forgot-password`; auth-recovery path planned in D-PLAN), **forgot-password ✓** ([forgot-password SPEC](specs/pages/web/forgot-password/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[web]`, **net-new** D-REF; always-send + always-visible `mailto:` contact fallback D-C1, inline email validation D-C2, reset via Express R1 D-C3; consumes `auth` v0.3.0 `POST /auth/forgot-password`), **reset-password ✓** ([reset-password SPEC](specs/pages/web/reset-password/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[web]`, **net-new** D-REF; recovery token via implicit-flow URL fragment D-C4, new+confirm+policy-hint form D-C3, Bearer→reuse `changePassword` D-C1, in-page success → `/login?reason=password-reset` D-C2; consumes `auth` v0.4.0 `POST /auth/reset-password`), **create-account ✓** ([create-account SPEC](specs/pages/web/create-account/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[web]`, faithful port + 5 deviations: inline email validation D-C1 (D-PLAN item 3), already-authed → `/programs` redirect D-C2, live password checklist D-C3, muted mismatch hint D-C4, autoFocus D-C5; register-then-auto-login → `/programs`; ported the `Select`/`SelectMobile` dependency; consumes `auth` `POST /auth/register` + `login()`), **privacy-policy ✓** ([privacy-policy SPEC](specs/pages/web/privacy-policy/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[web]`, the **PUBLIC** (pre-auth) Privacy Policy at `/privacy-policy` — **NOT** under the `middleware.ts` matcher so no auth guard/edge bounce; the **public twin of `program/privacy`** (byte-identical body) but a distinct access tier, header **Support** link; **faithful 1:1 D-S1 no deviations** + D-DUP keep the body DUPLICATED not single-sourced (F2 web↔web dup, user decision); **no new deps** — purer than `program/privacy` (no `useAuthGuard`); role N/A pre-auth; F1 shared iOS-push text on web, F3 hardcoded date/email; **no feature bump**), **support ✓** ([support SPEC](specs/pages/web/support/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[web]`, the **PUBLIC** (pre-auth) Support/Contact page at `/support` — the **smallest page in the rebuild** (38 legacy lines): a contact email + "what to include" list, header **Privacy Policy** link cross-linking the sibling; **NOT** under the matcher so no auth guard; **faithful 1:1 D-S1 no deviations**; **no new deps** (no `useAuthGuard`); role N/A pre-auth; F1 public/no-role, F2 hardcoded contact email differs from the policy's, F3 iOS-oriented include-list on web; **no feature bump**) — **the PUBLIC group is COMPLETE, and these two pages CLOSE THE ENTIRE WEB SURFACE (34/34 legacy pages ported; net-new pages since — 2 auth-recovery + `delete-account` — bring the index to 37; see `specs/pages/REGISTRY.md`)**
- [~] **programs hub ✓** ([programs SPEC](specs/pages/web/programs/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[web]`, **first protected route**; faithful 1:1 D-S1 + reuse `useAuthGuard` D-C3; **resolves middleware HS256→ES256** via D-C1 (edge = decode + expiry only, backend JWKS-verifies every call); ported deps verbatim D-C2: `lib/api/{programs,invites}.ts` + `ui/{PageShell,GlassCard,Modal,ConfirmDialog,StatusBadge}`) · program overview + roles/edit pending
- [~] **program settings hub ✓** ([program SPEC](specs/pages/web/program/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[web]`, **fourth & last workspace tab** `/program`; role-gated settings menu for admins / read-only program-info card + switch/leave for non-admins; only Leave + Sign Out live, all else forward-nav) · **edit ✓** ([program/edit SPEC](specs/pages/web/program/edit/SPEC.md) v0.1.0 — 🏗️ ported, admin-only details form → `PUT /programs/:id`; faithful 1:1 D-S1 + D-C1 client date-range validation + D-C2 hydrate active-program from server response + D-C3 skip no-op PUT; ported shared chrome `ui/PageHeader.tsx` + `BackButton.tsx`; consumes `programs` + `auth`, **no feature bump**) · **roles ✓** ([program/roles SPEC](specs/pages/web/program/roles/SPEC.md) v0.1.0 — 🏗️ ported, admin-only role-management list → `PUT /program-memberships`; faithful 1:1 D-S1 + D-C1 tokenize role-button colors (`rf-warning`/`rf-info`/`rf-text-muted`) + D-C2 optimistic role update + D-C3 disable all buttons while in flight; ported shared chrome `ui/LoadingState.tsx`; consumes `program-memberships` + `auth`, **no feature bump**) · **profile ✓** ([program/profile SPEC](specs/pages/web/program/profile/SPEC.md) v0.1.0 — 🏗️ ported, the signed-in user's **own "My Profile" account page** (first/last name + gender + Delete Account) — NOT a program-admin setting (no admin redirect; all roles; only Delete hidden from global_admin) → `PUT /members/:id` + `DELETE /auth/account`; faithful 1:1 D-S1 + D-C1 tokenize success color → `rf-success` + D-C2 clear stale success/error on field edit; **no new deps** (purest shape); consumes `members` + `auth`, **no feature bump**) · **password ✓** ([program/password SPEC](specs/pages/web/program/password/SPEC.md) v0.1.0 — 🏗️ ported, the signed-in user's **own change-password page** (new + confirm + live policy checklist) — NOT a program-admin setting (no admin redirect; all roles; no role-conditional UI) → `PUT /auth/change-password`; near-twin of the `reset-password` form minus the URL-fragment token; faithful 1:1 D-S1 + D-C1 tokenize success/checklist color → `rf-success` + D-C2 reuse `useAuthGuard` + D-C3 clear stale message on edit; **no new deps** (purest shape); consumes `auth`, **no feature bump**) · **appearance ✓** ([program/appearance SPEC](specs/pages/web/program/appearance/SPEC.md) v0.1.0 — 🏗️ ported, the signed-in user's **appearance/theme picker** (System/Light/Dark → `localStorage["rf:appearance"]`) — NOT a program-admin setting (no admin redirect; all roles; no role-conditional UI at all) → **no backend, no API call** (pure client-side); the write side of the contract the hub's appearance-label only reads (run-24 F5); faithful 1:1 D-S1 (already fully tokenized — no tokenize cleanup) + D-C1 reuse `useAuthGuard`; **no new deps** (purest shape — nothing dragged in); consumes only foundation `lib/theme.ts` + `useAuthGuard`, **no feature bump**) · **privacy ✓** ([program/privacy SPEC](specs/pages/web/program/privacy/SPEC.md) v0.1.0 — 🏗️ ported, the **Privacy Policy** static document — NOT a program-admin setting (no admin redirect; all roles; no role-conditional UI at all) → **no backend, no API, no client storage at all** (the purest page in the rebuild — static content, no state); faithful 1:1 D-S1 (content verbatim, already fully tokenized — no tokenize cleanup) + D-C1 reuse `useAuthGuard`; **no new deps**; consumes only foundation chrome + `useAuthGuard`, **no feature bump**) — **the `/program/*` sub-route group is now COMPLETE (6 of 6)**
- [x] **summary dashboard ✓** ([summary SPEC](specs/pages/web/summary/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[web]`, **first workspace tab** `/summary`; faithful 1:1 D-S1 + one typed cleanup D-C1 (`ProgramProgressCard` `any`→`AnalyticsSummary`); landing + the 3 desktop log-form modals this run per D-SCOPE; ported deps `lib/api/{summary,logs,program-workouts}.ts` + `ui/{ErrorState,Input,Button}` + `forms/{LogWorkoutForm,BulkLogWorkoutForm,LogDailyHealthForm}`) · **activity ✓** ([summary/activity SPEC](specs/pages/web/summary/activity/SPEC.md) v0.1.0 — 🏗️ ported, the **workout activity timeline** detail (`PeriodSelector` W/M/Y/P + a `BarChart` of workouts + active-members per bucket over `workout_logs`); **read-only, program-wide** — no view-as picker, no `memberId`, no role logic at all (F2) so `admin_only_data_entry` N/A; faithful 1:1 D-S1 + 2 chart cleanups (D-C1 `<Legend>` + series names, D-C2 empty-state guard; dual-axis NOT applied — both series are counts); **no new deps** (every import already ported — purest shape); consumes `analytics` (`GET /analytics/timeline`, `authenticateToken`-only) + `auth`, **no feature bump**) · **distribution ✓** ([summary/distribution SPEC](specs/pages/web/summary/distribution/SPEC.md) v0.1.0 — 🏗️ ported, the **workout distribution by day-of-week** detail (a single `BarChart` of total workout count per weekday Sun→Sat over `workout_logs`); **read-only, program-wide, all-time** — no period selector, no view-as picker, no `memberId`, no state, no role logic (F2) so `admin_only_data_entry` N/A; faithful 1:1 D-S1 + 1 cleanup (D-C1 all-zero empty-state guard; `<Legend>` + dual-axis NOT applied — single counts series); **no new deps** (purest shape — even purer than `activity`); consumes `analytics` (`GET /analytics/distribution/day`, `authenticateToken`-only) + `auth`, **no feature bump**) · **workout-types ✓** ([summary/workout-types SPEC](specs/pages/web/summary/workout-types/SPEC.md) v0.1.0 — 🏗️ ported, the **top workout types** detail (a single-series `BarChart` of sessions per workout type + a ranked `<ul>` list name·sessions·avg-min over `workout_logs`, program-to-date); **read-only, program-wide** — no period selector, no view-as picker, no `memberId`, no state, no role logic (F2) so `admin_only_data_entry` N/A; faithful 1:1 D-S1 + 1 cleanup (D-C1 styled empty-state panel matching `distribution`; predicate kept `data.length===0` — already correct for a variable-length array, no run-34 fix; `<Legend>` + dual-axis NOT applied — single counts series); **no new deps** (purest shape, same as `distribution`); consumes `analytics` (`GET /analytics/workouts/types`, `authenticateToken`-only) + `auth`, **no feature bump**) · **log-workout ✓** ([summary/log-workout SPEC](specs/pages/web/summary/log-workout/SPEC.md) v0.1.0 — 🏗️ ported, the **standalone Log-workout mobile fallback** (`PageShell` + `PageHeader` wrapping `LogWorkoutForm variant="page"` — member/workout/date/duration → `POST /workout-logs`); the **1st `/summary` sub-route where `admin_only_data_entry` is LIVE** (mount `router.replace("/summary")` lock guard for non-admins; backend `requireDataEntryAllowed` the real 403); role logic live — `canLogForAny` (global_admin/admin/logger) shows the member picker + may log for any member, a member is forced to "You"/own id; faithful 1:1 D-S1 + 1 cleanup (D-C1 deterministic `router.push("/summary")` over legacy `router.back()` at the 2 sites; lock `router.replace` unchanged); **no new deps** (the form + `addWorkoutLog` + chrome all landed with the summary landing — purest write-page shape); `consumed_by=[web]` (iOS native log screen); consumes `workout-logs` + `program-memberships`/`program-workouts` (form lookups) + `auth`, **no feature bump**) · **log-health ✓** ([summary/log-health SPEC](specs/pages/web/summary/log-health/SPEC.md) v0.1.0 — 🏗️ ported, the **standalone Log-daily-health mobile fallback** (`PageShell` + `PageHeader` wrapping `LogDailyHealthForm variant="page"` — member/date/sleep-time/diet-quality → `POST /daily-health-logs`); twin of `log-workout`, the **2nd `/summary` sub-route where `admin_only_data_entry` is LIVE** (mount `router.replace("/summary")` lock guard for non-admins; backend `requireDataEntryAllowed` the real 403); role logic live — `canLogForAny` shows the member picker + may log for any member, a member is forced to "You"/own id; faithful 1:1 D-S1 + 1 cleanup (D-C1 deterministic `router.push("/summary")` over legacy `router.back()` at the 2 sites; lock `router.replace` unchanged); **no new deps** (the form + `addDailyHealthLog` + chrome all landed with the summary landing — needs only the member lookup, no workout-types); `consumed_by=[web]` (iOS native log screen); consumes `workout-logs` (`POST /daily-health-logs`) + `program-memberships` (member lookup) + `auth`, **no feature bump**) · **bulk-log-workout ✓** ([summary/bulk-log-workout SPEC](specs/pages/web/summary/bulk-log-workout/SPEC.md) v0.1.0 — 🏗️ ported, the **standalone Bulk-log-workouts mobile fallback** (`PageShell` + `PageHeader` wrapping `BulkLogWorkoutForm variant="page"` — a ≤200-row table/cards of member/workout/date/duration → `POST /workout-logs/batch`); near-twin of `log-workout`/`log-health`, the **3rd & last `/summary` sub-route where `admin_only_data_entry` is LIVE**; **two-way mount redirect** (lock → `/summary` AND `!canLogForAny` → `/summary/log-workout`, the bulk-only member-bounce — bulk-logging is admin/logger-only, backend `logService.js:191-192` the real 403); per-row `ApiError.details → BulkRowError[]` plumbing; faithful 1:1 D-S1 + 1 cleanup (D-C1 deterministic `router.push("/summary")` over legacy `router.back()` at the 2 sites; both lock/role `router.replace` unchanged); **no new deps** (the form + `addWorkoutLogsBatch` + chrome all landed with the summary landing); `consumed_by=[web]` (iOS native log screen); consumes `workout-logs` (`POST /workout-logs/batch`) + `program-memberships`/`program-workouts` (per-row lookups) + `auth`, **no feature bump**) — **the `/summary` sub-route group is COMPLETE: 6 of 6 (all 3 chart drill-downs + all 3 log fallbacks)**
- [x] logging: **the 3 forms ✓** (ported with `summary` as desktop modals — `LogWorkoutForm`/`BulkLogWorkoutForm`/`LogDailyHealthForm`, write path live on `/summary`); **all 3 standalone mobile pages ✓** — `log-workout` + `log-health` + `bulk-log-workout` (render `LogWorkoutForm`/`LogDailyHealthForm`/`BulkLogWorkoutForm` `variant="page"` — 3 of 3)
- [~] **members dashboard ✓** ([members SPEC](specs/pages/web/members/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[web]`, **second workspace tab** `/members`; NOT roster-management — a per-member overview with a role-gated **"view as"** picker; read-only so `admin_only_data_entry` N/A; faithful 1:1 D-S1 + 2 cleanups: port whole `lib/api/members.ts` D-C1, hoist `formatDuration`→`lib/format.ts` D-C2, de-dup the two `MemberPickerModal` blocks D-C3; consumes `member-analytics` + `daily-health-logs` + `program-memberships` + `auth`) · **list ✓** ([members/list SPEC](specs/pages/web/members/list/SPEC.md) v0.1.0 — 🏗️ ported, the **active-member roster** behind the landing's "View Members" pill (1st of 8 `/members` sub-routes); searchable read-only list over `fetchMembershipDetails` (`GET /program-memberships/details`), client-filtered to `status==="active"`; **global_admin-only** row click → deferred `/members/detail`; read-only for all other roles → `admin_only_data_entry` N/A; faithful 1:1 D-S1 + D-C1 tokenize "Inactive" badge → `bg-rf-danger/10 text-rf-danger`; **no new deps** (`fetchMembershipDetails` ported with the `program` family, runs 24/26) + all chrome already ported; consumes `program-memberships` + `auth`, **no feature bump**) · **detail ✓** ([members/detail SPEC](specs/pages/web/members/detail/SPEC.md) v0.1.0 — 🏗️ ported, the **global_admin per-member editor** behind `members/list`'s row click (2nd of 8 `/members` sub-routes); identity card + editable Joined-Program date + Active-Membership checkbox → `updateMembership` (`PUT /program-memberships`, `joined_at`+`is_active` only) + Remove → `removeMember` (`DELETE`); both → push `/members/list`; **global_admin-only** mount redirect, `admin_only_data_entry` N/A (membership editing not logging); faithful 1:1 D-S1 + D-C1 `window.confirm`→`ConfirmDialog` + D-C2 tokenize Remove button → `bg-rf-danger/10 text-rf-danger` + D-C3 clear stale error on edit; **no new deps** (write fns already in `lib/api/programs.ts`, runs 24/26) + all chrome incl. `ConfirmDialog` already ported; consumes `program-memberships` + `auth`, **no feature bump**) · **invite ✓** ([members/invite SPEC](specs/pages/web/members/invite/SPEC.md) v0.1.0 — 🏗️ ported, the **program-admin invite-by-username form** behind the landing's "Invite Member" pill (3rd of 8 `/members` sub-routes); `@`-prefixed username `<input>` + privacy info-banner → `sendProgramInvite` (`POST /program-memberships/invite`); **program-admin / global_admin-only** mount redirect, `admin_only_data_entry` N/A (role-gated, not logging); privacy-safe **swallow-errors-as-success** kept faithful (F1); faithful 1:1 D-S1 + D-C1 tokenize success box → `bg-rf-success/10 text-rf-success` + D-C2 clear stale error on edit; **no new deps** (`sendProgramInvite` already in `lib/api/members.ts:204`, run 22) + all chrome already ported; consumes `invites` + `auth`, **no feature bump**) · **metrics ✓** ([members/metrics SPEC](specs/pages/web/members/metrics/SPEC.md) v0.1.0 — 🏗️ ported, the **program-wide Member Performance Metrics dashboard** behind the landing's metrics card (4th of 8 `/members` sub-routes); search + sort/direction `Select`s + a 9-range Filters modal over `fetchMemberMetrics` (`GET /member-metrics`, server-driven sort/filter/search) + a grid of per-member cards + client-side **Export CSV**; **no page-level role gate** — every role that reaches it sees the same grid, entry-link is program-admin-only on the landing (F2 asymmetry), backend `ensureProgramAccess` allows any active member (F3 secure); read + CSV only → `admin_only_data_entry` N/A; faithful 1:1 D-S1 + D-C1 full-tokenize amber flame badge → `bg-rf-warning/20 text-rf-warning`; **no new deps** (`fetchMemberMetrics` already in `lib/api/members.ts:102`, run 22) + all chrome already ported; consumes `member-analytics` + `auth`, **no feature bump**) · **history ✓** ([members/history SPEC](specs/pages/web/members/history/SPEC.md) v0.1.0 — 🏗️ ported, the **per-member Workout History timeline** behind the landing's History card (5th of 8 `/members` sub-routes); `PeriodSelector` W/M/Y/P over a single-series workouts `BarChart` over `fetchMemberHistory` (`GET /member-history`), target from URL `memberId`/`name`; **client per-member redirect** — staff (`canViewAny`) view any member, a plain member only their own (else `router.push("/members")`); read-only → `admin_only_data_entry` N/A; the redirect is **stricter than the backend** (`getMemberHistory` only `ensureProgramAccess` + target-enrolled — F2 mirror of run-40), per-program read authz IS enforced in the service (F3 secure); faithful 1:1 D-S1 + D-C1 all-zero empty-state guard (keyed off the sum — buckets always present, run-34 predicate; `<Legend>`/dual-axis NOT applied — single counts series); **no new deps** (`fetchMemberHistory` already in `lib/api/members.ts:126`, run 22; `PeriodSelector` ported run 32) + all chrome already ported; consumes `member-analytics` + `auth`, **no feature bump**) · **streaks ✓** ([members/streaks SPEC](specs/pages/web/members/streaks/SPEC.md) v0.1.0 — 🏗️ ported, the **per-member Streak Stats** detail behind the landing's Streak Stats card (6th of 8 `/members` sub-routes); Current/Longest day pills + a milestone badge ladder over `fetchMemberStreaks` (`GET /member-streaks`), target from URL `memberId`/`name`; **confirm-only simpler-twin of `members/history`** — same per-member scope + `canViewAny` redirect, SUBTRACT the chart (no `PeriodSelector`/`BarChart`/Recharts/`period`); **client per-member redirect** stricter than backend (F2 mirror), per-program read authz IS enforced in the service (F3 secure); read-only → `admin_only_data_entry` N/A; faithful 1:1 D-S1 + D-C1 non-color milestone affordance (`✓` + `ring` on achieved, vs color-only legacy — run-33 concern); **no new deps** (`fetchMemberStreaks` already in `lib/api/members.ts:131`, run 22; `metric-pill`/`rf-accent` already ported) + all chrome already ported; consumes `member-analytics` + `auth`, **no feature bump**) · **workouts ✓** ([members/workouts SPEC](specs/pages/web/members/workouts/SPEC.md) v0.1.0 — 🏗️ ported, the **per-member workout-log manager** behind the landing's Recent Workouts card (7th of 8 `/members` sub-routes); sort + Filter modal (date range / type / min-max duration) + **Export CSV** over `fetchMemberRecentWorkouts` (`GET /member-recent`) + per-row **Edit** (duration modal → `PUT /workout-logs`) / **Delete** (`ConfirmDialog` → `DELETE /workout-logs`), target from URL `memberId`/`name`; the **only WRITE sub-route** in the group → `admin_only_data_entry` is **LIVE** (gates `canEdit`/`canDelete`, run-31/36/40 read-vs-write axis); **client per-member redirect** stricter than the backend read path (F2 mirror); faithful 1:1 D-S1 + D-C1 `window.confirm`→`ConfirmDialog` (run 31) + D-C2 reuse hoisted `formatDuration` (run 22) + D-C3 tokenize Delete button → `bg-rf-danger/10 text-rf-danger` (run 39); **no new deps** (api fns across `members.ts`/`program-workouts.ts`/`logs.ts`, all ported runs 21/22; `ConfirmDialog`+all chrome ported) + lazy `program-workouts` filter options (F4), `limit:0`→1000 coercion (F1), no list-query error state (F5) flagged; consumes `member-analytics` + `program-workouts` + `workout-logs` + `auth`, **no feature bump**) · **health ✓** ([members/health SPEC](specs/pages/web/members/health/SPEC.md) v0.1.0 — 🏗️ ported, the **per-member daily-health log manager** behind the landing's "View Health" card (8th & LAST of 8 `/members` sub-routes); sort + Filter modal (date range / min-max sleep / min-max diet) + **Export CSV** over `fetchMemberHealthLogs` (`GET /daily-health-logs`) + per-row **Edit** (sleep + diet modal, 0:00–24:00 + at-least-one-metric validation → `PUT /daily-health-logs`) / **Delete** (`ConfirmDialog` → `DELETE /daily-health-logs`), target from URL `memberId`/`name`; the **WRITE twin of `members/workouts` (run 45)** → `admin_only_data_entry` is **LIVE** (gates `canEdit`, run-31/36/40/45 read-vs-write axis); **client per-member redirect** stricter than the backend read path (F2 mirror); faithful 1:1 D-S1 + D-C1 `window.confirm`→`ConfirmDialog` (run 45/31) + D-C2 tokenize Delete button → `bg-rf-danger/10 text-rf-danger` (run 45/39); **NO hoist cleanup** (unlike workouts' D-C2 — `sleepLabel`/`dietLabel` already shared in `format.ts`); **no new deps** (api fns across `members.ts`/`logs.ts`, ported runs 21/22; `ConfirmDialog`+all chrome ported) + `limit:0`→1000 coercion (F1), no list-query error state (F5), always-send `member_id` / no lazy filter query (deltas from the twin, F3/F4) flagged; consumes `member-analytics` + `daily-health-logs` + `auth`, **no feature bump**) — **the `/members` sub-route group is now COMPLETE (8 of 8)**
- [x] **lifestyle dashboard ✓** ([lifestyle SPEC](specs/pages/web/lifestyle/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[web]`, **third workspace tab** `/lifestyle`; NOT a sleep/diet logging screen — a **read-only** workout-type-analytics + health-timeline overview with the same role-gated **"view as"** picker as Members; read-only so `admin_only_data_entry` N/A; faithful 1:1 D-S1 + 2 verbatim dep ports: whole `lib/api/lifestyle.ts` D-C1, `ui/EmptyState.tsx` D-C2; consumes `analytics` + `analytics-v2` + `program-memberships` + `auth`, all endpoints already mounted, **no feature bump**) · **workouts ✓** ([lifestyle/workouts SPEC](specs/pages/web/lifestyle/workouts/SPEC.md) v0.1.0 — 🏗️ ported, the **workout-type management** screen (searchable Available/Hidden list of global + custom types; admins Add/Edit/Hide-Show/Delete, everyone else read-only); **admin-ROLE gated, NOT `admin_only_data_entry`** (corrects the run-23 forward-inference — F1) + non-admins get a **read-only degrade, not a redirect** (F2); faithful 1:1 D-S1 + D-C1 `window.confirm`→`ConfirmDialog` + D-C2 clear stale error on Add **and** Edit modal open; **no new deps** (every import already ported — purest shape); consumes `program-workouts` (all 6 routes/fns) + `auth`, **no feature bump**) · **timeline ✓** ([lifestyle/timeline SPEC](specs/pages/web/lifestyle/timeline/SPEC.md) v0.1.0 — 🏗️ ported, the **sleep + diet-quality health timeline** detail (`PeriodSelector` W/M/Y/P + a `ComposedChart` of sleep bars + diet line over `daily_health_logs`); **read-only** so `admin_only_data_entry` N/A; **no view-as picker** — scope from the URL `memberId` the landing passes, no admin redirect (F2); faithful 1:1 D-S1 + D-C1 port `ui/PeriodSelector.tsx` verbatim + 3 chart cleanups (D-C2 dual Y-axis, D-C3 `<Legend>`, D-C4 axis unit labels); consumes `analytics` (`GET /analytics/health/timeline`, `authenticateToken`-only) + `auth`, **no feature bump**) — **CLOSES the `/lifestyle` group (2 of 2)**
- [x] cross-cutting: auth/session, role-based UI, React Query layer, charts (all ported in the foundation +
  pages; the `notifications` web client landed run 48) — **the web surface is feature-complete + LIVE on
  `https://rasifiters.com`**

## ios (SwiftUI) — `ios-mobile`
- [x] **foundation scaffold (run 50, 2026-06-30)** — Xcode project + `App`/`Config`/`Shared/{Services,Theme,Models,Components,Views}` copied verbatim into `apps/ios`, API repointed, `Features/` deferred (4 views stubbed in `App/_DeferredScreenStubs.swift`). Code-present, build green-check owned by the user (Xcode). See `apps/ios/CONTEXT.md` §Foundation port.
- [x] auth: **splash ✓** ([splash SPEC](specs/pages/ios/splash/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[ios]`, the unauthenticated `AppRootView` branch; `SplashViewModel` typewriter intro + Sign-in CTA → `LoginView`; faithful 1:1 D-S1 **+ D-C1 real `BrandMark`** replacing the legacy placeholder (closes web splash F3); D-DEPS new `BrandMark.swift` + `BrandIcon.imageset`; role N/A pre-auth; no API), **login ✓** ([login SPEC](specs/pages/ios/login/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[ios]`, identifier+password+Show/Hide → `APIClient.loginGlobal` (`POST /auth/login/global`) → `ProgramContext` session + `loadLookupData`/`persistSession` → `ProgramPickerView`; faithful 1:1 D-S1 **+ D-C1 real `BrandMark`** **+ D-C2 "Forgot your password?" link** opening the live web recovery flow (`APIConfig.forgotPasswordURL`; closes web login F3 — reset always completes in-browser); consumes `auth`; role N/A pre-auth), **create-account ✓** ([create-account SPEC](specs/pages/ios/create-account/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[ios]`, full field set + gender `Menu` → `registerAccount` then `loginGlobal` (register-then-auto-login) → `ProgramPickerView`; faithful 1:1 D-S1 **+ 4 web-parity cleanups**: D-C2 inline email validation + muted hint, D-C3 live password checklist, D-C4 muted mismatch hint, D-C5 autoFocus First Name (+ D-C1 real `BrandMark`); web's authed-redirect cleanup N/A — the iOS root bifurcation handles it; consumes `auth`; role N/A pre-auth) — **the iOS public/auth path is COMPLETE (splash → login → create-account); `BrandMark`/`BrandIcon` + `APIConfig.forgotPasswordURL` added; `SplashView` deferred stub removed**
- [x] program picker + create/edit/invites — **picker ✓** ([program-picker SPEC](specs/pages/ios/program-picker/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[ios]`, the post-auth landing `AppRootView` shows once `authToken` is set; "My Programs" `List` of `ProgramCard`s (`fetchPrograms`), `canOpen`/`canManage` role gating, swipe edit/delete, inline invite Accept/Decline (`updateMembershipStatus`), floating "+" → `ProgramActionsSheet` with the pending-invite badge, inline `AccountMenuSheet`, `applyProgram` hydration → `AdminHomeView`, delete/sign-out `Alert`s; faithful 1:1 D-S1 **+ D-C1 visible error banner** (the legacy swallowed errors — web parity); **D-REF keep iOS-native multi-screen nav** (the web single-page-modal hub is a platform-idiom divergence, F7); D-DEPS no new dependency; role rules = `canOpen`/`canManage` table, `admin_only_data_entry` N/A). · **create/edit/invites ✓ (run 59)** — [program-actions SPEC](specs/pages/ios/program-actions/SPEC.md) v0.1.0 (the picker "+" sheet: `ProgramActionsSheet` tabbed Invites/Create + `CreateProgramTabView` + `InvitesTabView`+`DeclineInviteDialog` + `InviteCard`; faithful 1:1 D-S1 — both web + legacy iOS agree → faithful is web parity, NO ADD; D-DEPS no new dep, `StatusPill` reused from `ProgramPickerView`) + [edit-program SPEC](specs/pages/ios/edit-program/SPEC.md) v0.1.0 (the swipe-Edit editor: name/status/dates **+ web-parity admin-only-data-entry toggle D-C1** + the 3 web Edit cleanups D-C2 (date-range validation, hydrate-from-response, skip-no-op); D-DEPS one new API-layer dep — `adminOnlyDataEntry` on `APIClient.updateProgram`+`ProgramContext.updateProgram`+`ProgramUpdateResponse`; zero backend, no feature bump). Ported into `Features/Home/ProgramActions/`; both stubs removed. **The post-pick `AdminHomeView` (run 53) + the 4 account screens (run 58) already landed.**
- [~] admin home tabs: summary, members, lifestyle/workout-types, program (admin + standard variants) — **shell ✓** ([admin-home SPEC](specs/pages/ios/admin-home/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[ios]`, the 96-line `TabView` post-pick home shell `ProgramPickerView` pushes on `applyProgram`; 4 tabs (Summary single · Members/Lifestyle/Program role-bifurcated via `isProgramAdmin`), nested `Period` enum (W/M/Y/P), `.adaptiveTint()` + `navigationBarBackButtonHidden`; faithful 1:1 D-S1, **D-REF keep iOS-native `TabView`** (web's 4-top-level-routes nav is a platform-idiom divergence — tab set/order already match web), D-DEPS no new dependency; role rules = the Admin*/Standard* variant table, `admin_only_data_entry` N/A at the shell; pure nav, no API). · **Summary tab ✓** ([admin-summary SPEC](specs/pages/ios/admin-summary/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[ios]`, Tab 1 the iOS analogue of web `/summary`; a scrollable drag-reorderable grid of 10 cards (program-progress ring · MTD participation/workouts/duration/avg · activity-timeline + distribution Swift Charts · top workout types · 2 log action cards) over the already-ported analytics loaders; 3 files `Tabs/{AdminSummaryTab,SummaryCards,SummaryChartCards}.swift`; **D-SCOPE scope-cut-IS-the-run** (grid + 10 cards live, the 5 `NavigationLink` detail targets stubbed — the iOS analogues of the web `/summary` sub-routes); **D-REF keep iOS-native** (reorderable grid + native drill-downs vs web routes/modals); faithful 1:1 D-S1 **+ 2 web-parity ADDs**: D-C1 visible error banner (legacy swallowed it), D-C2 `admin_only_data_entry` landing treatment (🔒 banner + dimmed/disabled log cards — absent in legacy iOS; wired `ProgramDTO.admin_only_data_entry` → `ProgramContext.adminOnlyDataEntry`/`dataEntryLocked`); D-DEPS no new dep for the dashboard (`ScrollableBarChart`/`DistributionPoint`/`typeColor` ported with the cards); role rules = same for all roles, `admin_only_data_entry` LIVE on the landing). · **Members tab ✓** ([admin-members SPEC](specs/pages/ios/admin-members/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[ios]`, Tab 2 the iOS analogue of web `/members`; role-bifurcated `AdminMembersTab` (invite + metrics-preview + view-as-any picker + 5 member cards) / `StandardMembersTab` (logger logs-only view-as; member own-data); 4 files `Tabs/{AdminMembersTab,StandardMembersTab,MemberCards,MemberOverviewPicker}.swift` + `Shared/Components/GlassButton.swift`; **D-SCOPE scope-cut-IS-the-run** (the 2 tab bodies + 7 inline cards/picker live, the 6 forward-nav detail targets stubbed + `ActivityTimelineDetailView` stub extended — the iOS analogues of the web `/members` sub-routes); **D-REF keep iOS-native** (view-as sheet + `NavigationLink` push detail views vs web single-page hub); **faithful 1:1 D-S1 — NO web-parity ADD** (both web AND legacy iOS swallow load errors → faithful-swallow IS web parity, the run-53 shape; unlike run-52 ProgramPicker where only web surfaced); **D-DEPS ONE new dep** — `GlassButton` (28-line shared chrome, co-located in a deferred Detail file, not in the foundation) + `memberTimelinePoints` free func; read-only → `admin_only_data_entry` N/A; role rules = the Admin*/Standard* view-as table). · **Lifestyle tab ✓** ([admin-workout-types SPEC](specs/pages/ios/admin-workout-types/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[ios]`, Tab 3 the iOS analogue of web `/lifestyle`; role-bifurcated `AdminWorkoutTypesTab` (view-as picker + 4 workout-type stat cards + popularity card + lifestyle-timeline card) / `StandardWorkoutTypesTab` (own data only, no picker); 3 files `Tabs/{AdminWorkoutTypesTab,StandardWorkoutTypesTab,WorkoutTypesCards}.swift` + `Shared/Components/AccentChip.swift`; **D-SCOPE scope-cut-IS-the-run** (the 2 tab bodies + 7 cards live, the 2 forward-nav targets stubbed — `ViewWorkoutTypesListView` = web `/lifestyle/workouts` CRUD + `LifestyleTimelineDetailView` = web `/lifestyle/timeline`); **D-REF keep iOS-native** (icon-only dumbbell `GlassButton` vs web's labelled Manage/View pill — same destination, cosmetic idiom); **faithful 1:1 D-S1 — NO web-parity ADD** (both web AND legacy iOS swallow load errors → faithful-swallow IS web parity, the run-53/55 shape); **D-DEPS ONE small new dep** — `AccentChip` (13-line capsule chip co-located in the legacy helper file, not in the foundation; the popularity logic/components + `CardShell`/`ScrollableBarChart` already ported run 50/54); read-only → `admin_only_data_entry` N/A; role rules = the Admin*/Standard* view-as table). · **Program tab ✓** ([admin-program SPEC](specs/pages/ios/admin-program/SPEC.md) v0.1.0 — 🏗️ ported, `consumed_by=[ios]`, Tab 4 (the LAST) the iOS analogue of web `/program`; role-bifurcated `AdminProgramTab` (sectioned management menu: `ProgramInfoSection` + 3 management sections + `ProgramMyAccountSection`) / `StandardProgramTab` (read-only program-info card + Switch + Leave + `ProgramMyAccountSection`); 3 files `Tabs/{AdminProgramTab,StandardProgramTab,ProgramCards}.swift`; **D-SCOPE scope-cut-IS-the-run** (the 2 tab bodies + 2 light sections `ProgramInfoSection`/`ProgramMyAccountSection` + `sectionHeader`/`settingsRow` helpers live, the 3 heavy management sections stubbed — `ProgramMemberManagementSection` = web `/members/*`, `ProgramRoleManagementSection` = web `/program/roles`, `ProgramWorkoutTypesSection` = web `/lifestyle/workouts`); **D-REF keep iOS-native** (native multi-screen management push-screens + iOS-only Notifications+Support account rows vs web's flat card→sub-route hub — idiom, not a gap); **faithful 1:1 D-S1 — NO web-parity ADD** (both web AND legacy iOS surface only the Leave-mutation error + swallow read errors → faithful-swallow IS web parity, the run-53/55/56 shape) **+ 3 pinned cleanups** D-C1 de-dup the triplicated Leave logic into a shared `.leaveProgramConfirmation` modifier (mirrors web's `LeaveProgramButton`), D-C2 drop the dead `PlaceholderTab` struct, D-C3 tokenize the `.blue` section accents → `appBlue`; **D-DEPS one small dep class** — the `sectionHeader`/`settingsRow` helpers (co-located in the legacy helper file, not in the foundation) + the new `.leaveProgramConfirmation` modifier; read-only → `admin_only_data_entry` N/A; role rules = the Admin*/Standard* variant table, `canLeaveProgram = !isGlobalAdmin`). **THE 4-TAB HOME SHELL IS COMPLETE (Summary ✓ · Members ✓ · Lifestyle ✓ · Program ✓).** **Remaining DEFERRED layer** (after runs 58–64): the Members detail-view stubs ✓ (run 63) + the Lifestyle timeline stub ✓ (run 64) are DONE — only the **2 widget entry views** (`QuickAddWorkoutWidgetEntryView`/`QuickAddHealthWidgetEntryView`) remain. **`AdminHomeView` + all 4 tab bodies' deferred stubs removed.** (Account screens ✓ run 58, program create/edit/invites ✓ run 59, Summary details ✓ runs 60–61, **Program management sections ✓ run 62** — see next bullet.)
- [x] summary detail views (iOS) **— COMPLETE (5/5): 2 log forms ✓ (run 60) + 3 chart drill-downs ✓ (run 61)**. **log forms ✓ (run 60, 2026-06-30)** — the 2 Summary log-action-card targets ported into `apps/ios/.../Features/Home/Detail/` + `LogFormComponents.swift`; the 2 deferred stubs removed. **log-workout ✓** ([log-workout SPEC](specs/pages/ios/log-workout/SPEC.md) v0.1.0 — `AddWorkoutDetailView`, iOS analogue of web `/summary/log-workout`; member (picker/self-lock) + workout `SearchablePickerSheet` + date + duration hrs/min → `APIClient.addWorkoutLog` (`POST /workout-logs`) direct) · **log-health ✓** ([log-health SPEC](specs/pages/ios/log-health/SPEC.md) v0.1.0 — `AddDailyHealthDetailView`, twin; member + past/today date + sanitized sleep hrs/min (0:00–24:00) + diet 1–5 `Menu` + at-least-one-metric → `APIClient.addDailyHealthLog` (`POST /daily-health-logs`) direct). **D-SCOPE cohesive log-forms cluster** (both forms one run, run-58/59); **D-S1 faithful 1:1** (legacy iOS + web agree → faithful IS web parity) **+ 4 deviations**: D-C1 web-parity `admin_only_data_entry` mount guard (`task` `dismiss()` when `dataEntryLocked` — legacy had none; completes the run-54 lock story on the write path), D-C2 adopt shared `AppInputField`(+`keyboardType`)/`AppPrimaryButton` chrome, D-C3 success → bump `ProgramContext.summaryRefreshToken` (Summary reloads via `.onChange`) + `dismiss` (drop the success Alert; ≈ web `invalidateQueries(["summary"])`), D-C4 inline errors (drop health's error Alert). **D-DEPS no new view component** — 2 tiny foundation touches (`AppInputField.keyboardType` param + `ProgramContext.summaryRefreshToken` + `AdminSummaryTab.onChange`) + shared `LogFormComponents.swift`; every API method/DTO/`dataEntryLocked` already ported (run 50/54). **Role rules:** `canSelectAnyMember` (global_admin/admin/logger) picker vs member self-lock; **`admin_only_data_entry` LIVE (write path)** — loggers locked out too (`isProgramAdmin` excludes logger). `consumed_by=[ios]`, **no feature bump**. **Chart drill-downs ✓ (run 61, 2026-06-30)** — the 3 remaining Summary `NavigationLink` targets ported into `Detail/{ActivityTimelineDetailView,DistributionByDayDetailView,WorkoutTypesDetailView}.swift` + shared `ChartDetailComponents.swift`; the 3 deferred stubs removed (SPECs [summary-activity-detail](specs/pages/ios/summary-activity-detail/SPEC.md) / [summary-distribution-detail](specs/pages/ios/summary-distribution-detail/SPEC.md) / [summary-workout-types-detail](specs/pages/ios/summary-workout-types-detail/SPEC.md), v0.1.0). **D-REF keep iOS-native** — the FIRST run where legacy iOS is *richer* than web (native tap-callouts + scrollable Swift Charts + period selector + member scope + "Others" aggregation + breakdown rows vs web's flat recharts) → platform-idiom exception (run-52/53), NOT simplified toward web. **D-S1 faithful 1:1** (richness kept) **+ cleanups**: D-C1 all-zero empty-state guard on `DistributionByDayDetailView` (legacy lacked it; keyed off the sum since the endpoint always returns 7 keys — matches web + siblings), D-C2 trim `ActivityTimelineDetailView`'s 6 unused init providers → `initialPeriod`/`memberId`/`showActiveSeries`, D-C3 tokenize chart colors (`.orange`→`appOrangeStrong`/`appOrange`, `.purple`→`appPurple` — `light:`-identical, run-26 shape; retro-tokenized the run-54 cards for card↔detail consistency; workout-types uses palette `barColor` so N/A there). **D-DEPS new dep set** (breaks the no-new-dep streak) — `CalloutView`/`HeaderStats`/`HeaderHeightKey`/`clamp` + axis/callout helpers + `WorkoutTypeRow`, co-located in legacy deferred Detail files so never in the foundation (run-55/56); skipped `GlassButton` (ported run 55) + `HealthCalloutView`/`HealthHeaderStats` (future lifestyle-timeline run); reused run-54 `DistributionPoint`/`distributionPoints`/`typeColor`/`barColor`/`ScrollableBarChart`/`DistributionChartOverlay` + run-55 `memberTimelinePoints` (NOT redefined). **Read-only → `admin_only_data_entry` N/A** on all 3; no role-conditional UI (program-wide, matches web F2). **`ActivityTimelineDetailView` serves double-duty** — Summary (program-wide) + Members `MemberHistoryCard` (memberId-scoped, `showActiveSeries:false`), the iOS analogue of web's two separate pages. `consumed_by=[ios]`, **no feature bump**. Symbols grep-verified (each new type/func defined once, shared helpers single-sourced, 3 stubs removed). **All 5 Summary detail views are now complete.**
- [x] **program management sections (run 62, 2026-06-30)** — the 3 heavy `AdminProgramTab` sections (deferred at run 57) ported into `apps/ios/.../Features/Home/ProgramManagement/`, all 6 shared stubs removed (3 sections + `ProgramMembersListView` + `InviteMemberView` + `ViewWorkoutTypesListView` — the last 3 double-duty with the Members/Lifestyle tab nav, runs 55/56). **member-management ✓** ([program-member-management SPEC](specs/pages/ios/program-member-management/SPEC.md) v0.1.0 — `ProgramMemberManagementSection` + `ProgramMembersListView` roster + global-admin-only `MemberDetailEditView` (joined-date/active/remove → `updateMembership`/`removeMember`) + `InviteMemberView` (privacy-swallow send → `sendProgramInvite`); iOS analogue of web `/members/{list,detail,invite}`; faithful 1:1 D-S1 — the global-admin-only editor gate + invite privacy-swallow match BOTH legacy iOS AND web; `admin_only_data_entry` N/A) · **role-management ✓** ([program-role-management SPEC](specs/pages/ios/program-role-management/SPEC.md) v0.1.0 — `ProgramRoleManagementSection` (admin/logger lists, gated `canEditProgramData`) + `ManageRolesView` (per-member 3-role picker, last-active-admin guard → `updateMembership(role:)`); iOS analogue of web `/program/roles`; **D-REF KEEP iOS-NATIVE** — the per-member spinner lock + refresh-after kept over web's optimistic-write + rollback + cross-row disable-all (the finer-grained iOS pattern, run-61 "iOS richer → keep native"); `admin_only_data_entry` N/A) · **workout-types ✓** ([program-workout-types SPEC](specs/pages/ios/program-workout-types/SPEC.md) v0.1.0 — `ProgramWorkoutTypesSection` + `ViewWorkoutTypesListView` (searchable Available/Hidden CRUD, swipe Show-Hide/Edit/Delete, Add/Edit sheets) + `EditCustomWorkoutSheet`; iOS analogue of web `/lifestyle/workouts`; faithful 1:1 D-S1 — admin-ROLE gated with read-only degrade (not redirect) matches BOTH clients; `admin_only_data_entry` N/A). **D-SCOPE cluster-IS-the-run** (all 3 sections + embedded detail views one run — they can't be split off, the detail-view stubs are shared with the ported Members/Lifestyle tabs); **D-DEPS no new dependency** (every API fn/DTO/`ProgramContext` wrapper/theme/`sectionHeader`/`settingsRow` already ported); **+ 2 cross-platform cleanups**: D-C1 tokenize bare colors (`.orange`/`.blue`/`.green`/`.purple`/`.red` → app tokens, light-mode-safe run-26/61), D-C2 clear-stale-error-on-edit (web members/detail D-C3 + invite D-C2 + workouts D-C2). `consumed_by=[ios]`, **no feature bump** (page SPECs at v0.1.0; every backend endpoint pre-exists — web consumes them).
- [x] **member detail: metrics, streaks, recent, health (run 63, 2026-06-30)** — the 4 remaining deferred Members detail-view stubs (reached from the Members tab cards, run 55) ported into `apps/ios/.../Features/Home/Detail/`, all 4 stubs removed. (Member **history** = `ActivityTimelineDetailView` memberId-scoped, already ported run 61.) **member-metrics ✓** ([member-metrics-detail SPEC](specs/pages/ios/member-metrics-detail/SPEC.md) v0.1.0 — `MemberMetricsDetailView` + co-located `MetricsFilters`/`SortSheet`/`FilterSheet`; searchable + server-sorted/filtered `MemberMetricsCard` grid over `loadMemberMetrics` + client CSV; web `/members/metrics`; **read-only** → `admin_only_data_entry` N/A; faithful 1:1 D-S1; `SortField`/`SortDirection`/`MemberMetricsCard` reused from run 55, NOT redefined) · **member-streaks ✓** ([member-streaks-detail SPEC](specs/pages/ios/member-streaks-detail/SPEC.md) v0.1.0 — `MemberStreakDetail` + `WrapChips`; current/longest tiles + milestone ladder over server-computed `memberStreaks`; web `/members/streaks`; **read-only** → lock N/A; faithful **+ D-C1 tokenize the one `.orange` → `appOrange`** + **D-C2 non-color ✓ milestone affordance** (web streaks D-C1, a11y); no in-view member-own redirect — gated upstream at the card, F3) · **member-recent ✓** ([member-recent-detail SPEC](specs/pages/ios/member-recent-detail/SPEC.md) v0.1.0 — `MemberRecentDetail` + sort/filter sheets + `WorkoutLogEditSheet` (relocated from the legacy health file); sorted/filterable workout list + swipe Edit(duration)/Delete + lazy `program-workouts` type picker + client CSV; web `/members/workouts`; **WRITE** → `admin_only_data_entry` **LIVE**) · **member-health ✓** ([member-health-detail SPEC](specs/pages/ios/member-health-detail/SPEC.md) v0.1.0 — `MemberHealthDetail` + sort/filter sheets + `DailyHealthEditSheet`; the write twin of member-recent — sleep+diet list + swipe Edit(at-least-one-metric, 0:00–24:00)/Delete + client CSV; web `/members/health`; **WRITE** → lock **LIVE**). **D-SCOPE cluster-IS-the-run** (all 4 one run, run-58→62 precedent); **D-C1 the web-parity `admin_only_data_entry` lock on the 2 write views** — swipe Edit/Delete gated on `!programContext.dataEntryLocked` (= `adminOnlyDataEntry && !isProgramAdmin`, run 54/60); legacy iOS had NO lock handling (relied on the backend 403) → completes the run-54/60 lock arc on the detail write path (list stays read-only, mutations hidden — matches web `isDataEntryLocked`); **D-DEPS no new dependency** (every API fn/DTO/`dataEntryLocked`/`ShareSheet`/`SearchablePickerSheet`/theme already ported run 50/54/55; view-local enums+sheets port co-located); **D-REF keep iOS-native** (NavigationLink push + native sheets vs web routes/modals — idiom, not a gap). `consumed_by=[ios]`, **no feature bump** (page SPECs v0.1.0; every backend endpoint pre-exists — web consumes them). 20 new types grep-verified defined-once, all 4 stubs removed.
- [x] **lifestyle timeline detail (run 64, 2026-06-30)** — the last non-widget deferred stub (`LifestyleTimelineDetailView`, reached from the Lifestyle tab timeline card, run 56) ported into `apps/ios/.../Features/Home/Detail/LifestyleTimelineDetailView.swift`; the 2 co-located health helpers (`HealthHeaderStats` + `HealthCalloutView`) added to `Detail/ChartDetailComponents.swift`; the deferred stub removed. **lifestyle-timeline ✓** ([lifestyle-timeline SPEC](specs/pages/ios/lifestyle-timeline/SPEC.md) v0.1.0 — the interactive sleep-hours-bars + diet-quality-line timeline over `GET /analytics/health/timeline` with a period selector (W/M/Y/P), daily-average `HealthHeaderStats`, and a native drag tap-callout (`HealthCalloutView`); always member-scoped (own id / admin-selected id from the card); web `/lifestyle/timeline`; **read-only** → `admin_only_data_entry` N/A). **D-REF keep iOS-native** — richer than web (native callout + horizontal scroll + period selector), the run-52/53/61 platform-idiom exception. **D-S1 faithful 1:1 native + 4 web-parity chart deviations** (legacy already tokenized colors → no tokenize cleanup): **D-C1 dual Y-axis** (web run-32 D-C2 analogue — legacy shared ONE axis so the diet 1–5 line sat flattened under the sleep bars; the port scales diet onto its own 0–5 axis: sleep leading, diet trailing), **D-C2 web-parity error banner** (legacy set `errorMessage` but rendered it nowhere; web surfaces `ErrorState` — run-52/54 shape), **D-C3 axis unit labels** ("hrs" leading / "/ 5" trailing, web D-C4), **D-C4 chart Legend** (Sleep/Diet via `chartForegroundStyleScale`, web D-C3 — user-picked despite the header/callout already labeling both series). **D-DEPS small new dep** — the 2 co-located health helpers only; the API method/DTO/loader (`fetchHealthTimeline`/`HealthTimelineResponse`/`loadHealthTimeline`, run 50), `ScrollableBarChart` (run 54), and every shared chart helper (`HeaderHeightKey`/`clamp`/`axisValues`/`shortLabel`/`calloutTitle`/`rangeLabel`, run 61) reused-not-redefined. Role rules = no role-conditional UI (scope from the card's `memberId`, gated upstream run 56); `consumed_by=[ios]`, **no feature bump** (page SPEC v0.1.0; endpoint pre-exists — web consumes it). 3 new types grep-verified defined-once, stub removed. **The iOS DEFERRED layer is now down to the 2 widget entry views.**
- [x] **settings: profile, password, appearance, notifications (run 58, 2026-06-30)** — the 4-screen account/settings cluster (`ProgramMyAccountSection` targets, run 57), all ported into `apps/ios/.../Features/Home/Settings/` + the 4 deferred stubs removed. **my-profile ✓** ([my-profile SPEC](specs/pages/ios/my-profile/SPEC.md) v0.1.0 — own-profile editor: header + first/last name + gender `Menu` + Save (`updateMemberProfile` `PUT /members/:id`) + Delete (`deleteAccount` `DELETE /auth/account`, gated `!isGlobalAdmin`); **D-C1 web-parity email-change ADD** (legacy iOS had no email field — new `APIClient.changeEmail` `PUT /auth/email` + `ProgramContext.changeEmail` + optional `MemberDTO.email` via `fetchMemberById`, mirrors web `/program/profile` D-EMAIL); **D-C3 adopt shared `AppInputField`/`AppPrimaryButton`/`AppDestructiveButton`**; faithful D-S1 otherwise; role = every role edits own profile, Delete hidden for global_admin, `admin_only_data_entry` N/A). **change-password ✓** ([change-password SPEC](specs/pages/ios/change-password/SPEC.md) v0.1.0 — new+confirm form (Show/Hide on new only) → `changePassword` (`PUT /auth/change-password`); **D-C2 web-parity 5-rule live policy checklist** (≥8/upper/lower/number/match) replacing the legacy 6-char hint — matches web `/program/password` + run-51 create-account + the backend `validatePassword`; **D-C3 shared components**; faithful D-S1; no role-conditional UI; `admin_only_data_entry` N/A). **appearance ✓** ([appearance SPEC](specs/pages/ios/appearance/SPEC.md) v0.1.0 — System/Light/Dark device-local picker (`ThemeManager` → `UserDefaults`) + preview; **faithful 1:1 verbatim** (D-S1, already matches web `/program/appearance`); D-C3 component-adoption N/A — bespoke selection controls, no inputs; no API; no role read). **notifications ✓** ([notifications SPEC](specs/pages/ios/notifications/SPEC.md) v0.1.0 — **iOS-only**, no web sibling; OS-authorization status card + Enable/Open-Settings (`UNUserNotificationCenter` + `registerForRemoteNotifications`); **faithful 1:1 verbatim** (D-S1); no direct API — the APNs `PUT /notifications/device` registration is owned by the `notifications` feature; no role read). **D-DEPS = one new API-layer dep** (`APIClient.changeEmail` + `ChangeEmailResponse` + `ProgramContext.changeEmail` + optional `MemberDTO.email` — the email ADD); no new view component (all chrome ported run 50). **The 4 `_DeferredScreenStubs.swift` stubs removed.**
- [x] **widgets: quick-add workout, quick-add health (run 65, 2026-06-30)** — the **last 2 deferred stubs**, ported into `apps/ios/.../Features/Widgets/` (+ shared `WidgetQuickAddComponents.swift`); `App/_DeferredScreenStubs.swift` **deleted** → **the iOS DEFERRED layer is CLOSED**. **quick-add-workout ✓** ([widget-quick-add-workout SPEC](specs/pages/ios/widget-quick-add-workout/SPEC.md) v0.1.0) + **quick-add-health ✓** ([widget-quick-add-health SPEC](specs/pages/ios/widget-quick-add-health/SPEC.md) v0.1.0) — the Home-Screen widget deep-link targets (`WidgetRoute.quickAdd{Workout,Health}`, presented by `AppRootView`): **multi-program** forms that log the SAME workout/health across every selected active program with shared-member/-workout **intersection**, per-program **rollback** on partial failure, and **exit-to-My-Programs**. **iOS-ONLY** (`consumed_by=[ios]`; no web sibling — web's bulk-log is multi-row/single-program) → faithful-to-legacy-only, no parity reconcile (run-58 shape). **D-SCOPE twin cluster = one run** (run 58/60/63); **D-S1 faithful 1:1** + **3 change-now deviations** (user-picked): **D-C1 per-program `admin_only_data_entry` lock** (net-new `widgetProgramLockedForLogging` = `admin_only_data_entry && !isProgramAdmin` per `ProgramDTO`; locked programs disabled + lock-badged + un-selectable — completes the run-54/60/63 lock arc onto the widget path, no legacy/web reference), **D-C2 shared chrome** (`LogFieldLabel`/`LogFieldRow`/`AppInputField`/`AppPrimaryButton` — appOrange/appBlue CTAs → unified label-capsule), **D-C3 shared scaffold** `WidgetQuickAddComponents.swift` (header · program selector · member field · success toast · member option · lock helper — the widget analogue of run-60 `LogFormComponents.swift`); **D-DEPS no new foundation dep** (all API/DTO/`ProgramContext`/chrome pre-existed run 50/60), **no feature bump**. `admin_only_data_entry` LIVE **per program** (write path); role rules = `canSelectAnyMember` picker vs member self-lock. **Native build GREEN via the xcode MCP (0 errors)** + symbols grep-verified; both stubs removed, `_DeferredScreenStubs.swift` deleted.
- [~] cross-cutting: ProgramContext state, APIClient + Keychain, SSE + push, version check — **foundation infra ported (run 50)**; per-screen wiring lands with each feature. **`BrandMark` component + `BrandIcon` asset added (run 51)** — the real-brand-icon parity with web (closes the placeholder bug across all auth screens; reused by every later screen with a logo)
- [x] **NET-NEW (not legacy parity): Apple Health auto-sync (runs 66→74)** → [apple-health SPEC](specs/features/apple-health/SPEC.md) (v0.5.0; iOS-only HealthKit workout **+ sleep** auto-sync ported from `vinaySankar2004/RaSi-Fiters` PR #4, corrected for our curated `workouts_library`; per-program date-window scoping + gated first-sync confirmation + admin-lock-aware sync; screen SPEC [ios/apple-health](specs/pages/ios/apple-health/SPEC.md)). **Outside the legacy-parity checklist above** — tracked via its feature SPEC, not a legacy route.
