package com.app.rasifiters.core.theme

import androidx.compose.material3.Typography

// Default Material3 type scale for now; brand type refinements land with the component kit.
val AppTypography = Typography()
